[build]
  command = "npm run build"
  publish = "dist"

# Force permanent 301 redirect removing trailing slashes
[[redirects]]
  from = "/*/"
  to = "/:splat"
  status = 301
  force = true

# Fallback for React Router (Single Page App)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200